---
  - name: "Thinkpad configurations and installation"
    hosts: localhost
    connection: local 
    become: true

    vars_files:
      - /mnt/ansible/vars.vault.yml
      - /mnt/ansible/ssh.config.tar.gz

    roles:
      - configs
      - apps
      - docker
